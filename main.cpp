#include <bits/stdc++.h>
#include "src.hpp"

int main() {
  // For this problem, the OJ will compile this repository and use the
  // resulting executable `code`. We do not perform runtime I/O here.
  // The public test drivers include this header directly when comparing.
  return 0;
}

